[project]
name = "a2-mini-crm-test"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.140.13",
    "uvicorn>=0.51.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
]
