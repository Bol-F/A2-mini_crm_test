# Leave empty in local development so requests use the Vite /api proxy.
VITE_API_BASE_URL=

# Optional development proxy target. It is not bundled into production code.
VITE_PROXY_TARGET=http://127.0.0.1:8000
